
@{
    ViewBag.Title = "Consulta";
}

<div class="container">
    <h2>Consulta tipo Identificacion</h2>
    <!-- begin row -->
    <div class="row">
        <div class="col-md-12">
            <!-- begin panel -->
            <div class="panel panel-inverse">
                <div class="panel-heading">
                    <h4 class="panel-title">Listado de Tipo de Identificacion</h4>
                </div>
                <div class="alert alert-warning fade in">
                    <button type="button" class="close" data-dismiss="alert">
                        <span aria-hidden="true">&times;</span>
                    </button>
                    Esta catalogo describe los tipo de de documentación valida que acredita a una persona, a esta clasificacion se hace referencia cuando se da de alta a un empleado.
                </div>
                <div class="panel-body">
                    <table id="data-table" class="table table-striped table-bordered">
                        <thead>
                            <tr>
                                <th>Id</th>
                                <th>Descripción</th>
                                <th>Estatus</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in ViewBag.listaIden)
                            {
                                <tr class="odd gradeX">

                                    <td><a href="#" onclick="modificar('@item.idIdentificacion')">@item.idIdentificacion </a></td>
                                    <td>@item.descripcion</td>
                                    <td>@item.estatus</td>

                                    @*@{var st = int.Parse(@item.estatus);}

                                        @if (st == 1)
                                        {
                                        <td>
                                            <p class="m-b-1"><input type="checkbox" data-render="switchery" data-theme="default" checked /></p>
                                        </td>
                                        }
                                        else
                                        {
                                        <td>
                                            <p class="m-b-1"><input type="checkbox" data-render="switchery" data-theme="default" /></p>
                                        </td>
                                        }*@
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
<script>


    function modificar(idTipoIdentidicacion) {
        window.location.href = '@Url.Action("Modificar", "TipoIndentificacion")' + '/' + idTipoIdentidicacion;
    }
</script>

