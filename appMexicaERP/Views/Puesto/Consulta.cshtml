
@{
    ViewBag.Title = "Consulta";
}
<div class="container">
    <h2>Consulta Categoria Puesto</h2>
    <!-- begin row -->
    <div class="row">
        <div class="col-md-12">
            <!-- begin panel -->
            <div class="panel panel-inverse">
                <div class="panel-heading">
                    <h4 class="panel-title">Listado de Categoria (Puestos) Mexica Tours</h4>
                </div>
                <div class="alert alert-warning fade in">
                    <button type="button" class="close" data-dismiss="alert">
                        <span aria-hidden="true">&times;</span>
                    </button>
                    Esta catalogo describe las catagorias o puestos dentro del coportativo, asi como los alcances del puesto, las horas a trabajar y el costo que este representa.
                </div>
                <div class="panel-body">
                    <table id="data-table" class="table table-striped table-bordered">
                        <thead>
                            <tr>
                                <th>Id</th>
                                <th>Categoria</th>
                                <th>Descripción</th>
                                <th>Alcance</th>
                                <th>Horas</th>
                                <th>Costo</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in ViewBag.listaPuestos)
                            {
                                <tr class="odd gradeX">

                                    <td><a href="#" onclick="modificar('@item.idPuesto')">@item.idPuesto </a></td>
                                    <td>@item.categoria</td>
                                    <td>@item.descripcion</td>
                                    <td>@item.alcancePuesto</td>
                                    <td align="center">@string.Format("{0,2:f}", item.horas)</td>
                                    <td align="right">@String.Format("{0,14:c}", item.costo)</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
<script>


        function modificar(idPuesto)
    {
            //return window.location.href='{{url("hotel/modificar")}}/'+id_hotel;
            window.location.href = '@Url.Action("Modificar", "Puesto")' + '/' + idPuesto;
    }
</script>