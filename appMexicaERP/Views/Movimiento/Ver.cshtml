
@{
    ViewBag.Title = "Ver";
}

<div class="container">
    <div class="row">
        <!-- begin col-10 -->
        <div class="col-md-12">
            <!-- begin panel -->
            <div class="panel panel-inverse">
                <div class="panel-heading">
                    <div class="panel-heading-btn">
                        <a href="javascript:;" class="btn btn-xs btn-icon btn-circle btn-default" data-click="panel-expand"><i class="fa fa-expand"></i></a>
                        <a href="javascript:;" class="btn btn-xs btn-icon btn-circle btn-success" data-click="panel-reload"><i class="fa fa-repeat"></i></a>
                        <a href="javascript:;" class="btn btn-xs btn-icon btn-circle btn-warning" data-click="panel-collapse"><i class="fa fa-minus"></i></a>
                    </div>
                    <h4 class="panel-title">Lista de movimientos</h4>
                </div>
                 <!--<div class="alert alert-warning fade in">
                    <button type="button" class="close" data-dismiss="alert">
                        <span aria-hidden="true">&times;</span>
                    </button>
                   The Buttons extension for DataTables provides a common set of options, API methods and styling to display buttons on a page that will interact with a DataTable. The core library provides the based framework upon which plug-ins can built.
                </div>-->
                <div class="panel-body">
                    <div class="row">
                        <div class="col-md-3">
                            <label>Bancos</label>
                            <select name="selectBancos" id="selectBancos" class="form-control input-sm" data-size="10" data-live-search="true" data-style="btn-success" onchange="paginaAjax('selectNegocio', { id : this.value }, '@Url.Action("VerPorIdBancoConAjaxParaMovimiento", "Empresa")', 'POST', null, null, false, true);">
                                <option value="">Seleccione un Banco</option>
                                @foreach (var item in ViewBag.listaBancos)
                                {
                                    <option value="@item.idBanco">@item.nombre</option>
                                }
                            </select>
                        </div>
                        <div class="row">
                            <div class="col-md-3">
                                <label>Empresas</label>
                                <select name="selectNegocio" id="selectNegocio" class="form-control input-sm" data-size="10" data-live-search="true" data-style="btn-default" onchange="paginaAjax('selectCuentas', { id : this.value}, '@Url.Action("VerPorIdCuentaConAjaxParaMovimiento", "Cuenta")', 'POST', null, null, false, true)"></select>
                            </div>
                            <div class="col-md-3">
                                <label>Cuentas</label>
                                <select name="selectCuentas" id="selectCuentas" class="form-control input-sm limpiarCampos" data-size="10" data-live-search="true" data-style="btn-default"  onchange="paginaAjax('dataTableListaMovimiento', { id : this.value}, '@Url.Action("ListarMovimientosPorCuentaConAjax", "Movimiento")', 'POST', null, null, false, true)">

                                </select>
                            </div>
                            
                        </div>
                    </div>

                    <br />
                    <table id="data-table" class="table table-striped table-bordered">
                        <thead>
                            <tr>
                                <th tyle="text-align:center;">FECHA</th>
                                <th tyle="text-align:center;">DESCRIPCIÓN</th>
                                <th tyle="text-align:center;">DEPOSITO</th>
                                <th tyle="text-align:center;">RETIRO</th>
                                <th tyle="text-align:center;">SALDO ACTUAL</th>
                                <th tyle="text-align:center;">OPCIONES</th>
                            </tr>
                        </thead>
                        <tbody id="dataTableListaMovimiento">
                           
                        </tbody>
                    </table>
                </div>
            </div>
            <!-- end panel -->
        </div>
        <!-- end col-10 -->
    </div>

</div>

<script>
    function ModificarMovimiento(idMovimiento)
    {
        window.location.href = '@Url.Action("Modificar", "Movimiento")' + '/' + idMovimiento;

    }
</script>